yum install docker -y

apt update
apt install docker.io -y


Docker Service Commands

systemctl start docker
systemctl stop docker
systemctl enable docker
systemctl status docker


🔥 Super short (interview rapid fire)
yum install docker -y → install
systemctl start docker → start service
docker pull → download image
docker run → create + start container
docker ps → running containers
docker stop/start → control
docker rm / rmi → delete

Architecture 
🔁 Flow (Easy understanding)
You run command → docker run nginx
Client → Daemon ki request pampisthundi
Daemon → Registry (Docker Hub) nundi image pull chesthundi
Container create chesi run chesthundi


Images
docker pull nginx
docker images
docker rmi nginx


Container
docker run -d --name  -p 80:80 nginx
docker ps
docker ps -a
docker stop <container_id>
docker start <container_id>
docker rm <container_id>

